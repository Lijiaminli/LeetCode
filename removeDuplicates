class Solution:
    def removeDuplicates(self, nums):
        """
        python3
        :type nums: List[int]
        :rtype: int
        """
        i = 0
        while i < len(nums)-1:
            if nums[i] == nums[i+1]:
                del nums[i]
            else:
                i = i + 1
